# This is a Declarative Automation Bundle definition for DAB_Project.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: DAB_Project
  uuid: 27dfbc1c-ddda-4b23-9d50-c7aaef20032d

include:
  - resources/*.yml

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://adb-1290381263006333.13.azuredatabricks.net
      root_path: /Workspace/Users/tomydata@tsarmirgmail.onmicrosoft.com/DE_Fusion/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: data_engineering_001
      schema: dev
  prod:
    mode: production
    workspace:
      host: https://adb-1290381263006333.13.azuredatabricks.net
      # We explicitly deploy to /Workspace/Users/tomydata@tsarmirgmail.onmicrosoft.com to make sure we only have a single copy.
      root_path: /Workspace/Users/tomydata@tsarmirgmail.onmicrosoft.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: data_engineering_001
      schema: prod
    permissions:
      - user_name: tomydata@tsarmirgmail.onmicrosoft.com
        level: CAN_MANAGE
